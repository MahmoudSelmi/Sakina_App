# تشغيل المشروع على Android وiOS

الزيب ده دلوقتي فيه **مشروع فلاتر كامل** (مش lib بس): مجلدي `android/`
و`ios/` جاهزين بكل الإعدادات اللازمة مُطبّقة فيهم مسبقًا (صلاحيات، تشغيل
في الخلفية، أيقونة التطبيق، الإشعارات..). محتاج خطوة واحدة بس تكمّل بيها
المشروع.

## الخطوة الوحيدة المطلوبة منك

بعض ملفات مشروع Xcode (زي `Runner.xcodeproj`) عبارة عن ملفات معقّدة
Xcode بيولّدها بنفسه ومحفوفة بمخاطر لو اتعدّلت يدويًا غلط (ممكن تتكسر
وميفتحش المشروع خالص). عشان كده سيبتها للأداة الرسمية بدل ما أخاطر
بملف يبان سليم بس فعليًا باظ.

من جوه مجلد المشروع، شغّل الأمر ده **مرة واحدة بس**:

```bash
flutter create .
```

الأمر ده آمن تمامًا على مشروع موجود بالفعل - **مش هيلمس ولا حرف** في
`lib/` أو أي ملف موجود مسبقًا (زي `AndroidManifest.xml` أو `Info.plist`
اللي ظبطتهملك بالفعل). هو بس هيكمّل الأجزاء الناقصة اللي محتاجة أداة
Xcode/Gradle الرسمية تولّدها (زي `Runner.xcodeproj` و`local.properties`).

بعد كده:

```bash
flutter pub get
flutter run
```

## اللي اتظبط ليك بالفعل جوه android/ وios/

- **صلاحيات كاملة**: إنترنت، تشغيل خلفية، إشعارات، تنبيهات مجدولة
- **خدمة audio_service** متسجلة في AndroidManifest (شاشة القفل + إشعار
  التحكم) - ده كان سبب مشكلة "التطبيق مش بيفتح" قبل كده
- **أيقونة تطبيق** بتصميم الشعار (تدرج ذهبي + قوس المحراب) لكل الأحجام
  المطلوبة في Android وiOS
- **UIBackgroundModes** لـ iOS (audio) عشان التشغيل يفضل شغال والشاشة
  مقفولة
- **فايربيز مفعّل بشكل شرطي**: البلجن بتاعه (`google-services`) بيتفعّل
  بس لو لقى ملف `google-services.json` حقيقي موجود - يعني مبيكسرش
  البناء لو لسه معملتش الخطوات في القسم اللي جاي

## لو عاوز تفعّل فايربيز (اختياري)

1. اعمل مشروع على https://console.firebase.google.com
2. ثبّت الأداة: `dart pub global activate flutterfire_cli`
3. شغّل من جوه المشروع: `flutterfire configure`
   (ده هيولّد `lib/firebase_options.dart` بمفاتيحك الحقيقية، وينزّل
   `google-services.json` في `android/app/` و`GoogleService-Info.plist`
   في `ios/Runner/` تلقائيًا)
4. فعّل "Email/Password" من Authentication > Sign-in method

## بخصوص أصوات الطبيعة (المطر والرياح)

**ملحوظة مهمة**: في فلاتر، ملفات الصوت (زي `assets/ambient/rain.mp3`)
**مش بتتحط يدويًا جوه مجلد android منفصل**. هي متسجلة في `pubspec.yaml`
تحت `assets:`، وأداة البناء بتاعة فلاتر بتحزّمها تلقائيًا جوه كل من
Android وiOS وقت البناء - ده السلوك الصحيح والمعتاد، ومفيش أي خطوة يدوية
إضافية مطلوبة منك. الملفات فعلًا موجودة وجاهزة في `assets/ambient/`
ومسجّلة صح.

## قبل النشر الفعلي على المتجرين

- **Android**: `android/app/build.gradle` فيه توقيع مؤقت بمفتاح debug
  عشان تقدر تجرّب `--release` على طول. قبل الرفع على Google Play، لازم
  تعمل مفتاح توقيع حقيقي خاص بيك (راجع توثيق فلاتر الرسمي: "Build and
  release an Android app").
- **iOS**: هتحتاج تفتح المشروع في Xcode وتظبط Team/Signing بحساب
  Apple Developer بتاعك قبل الأرشفة والرفع على App Store.
